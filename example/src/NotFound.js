import React, { Component } from 'react'

import {Router} from 'react-angular-router'

export default class NotFound extends Component {
  render () {
    return (
      <div>
        not found :(((((
      </div>
    )
  }
}
